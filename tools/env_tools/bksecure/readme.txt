[ how to install setup.py ]
1. Build the installation package
```python setup.py sdist bdist_wheel```

2. Install the package to the system environment
```pip install dist/beken_utils-0.1.0-py3-none-any.whl```
