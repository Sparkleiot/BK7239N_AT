armino_build_get_property(target ARMINO_SOC)

set(incs)
set(srcs)

armino_component_register(SRCS "${srcs}" INCLUDE_DIRS "${incs}")

if (CONFIG_BL2)

armino_build_get_property(partition_hdr_dir SECURITY_PREBUILD_DIR)
set(bl2_build_dir ${CMAKE_BINARY_DIR}/armino/bl2)

include(ExternalProject)
ExternalProject_Add(bl2_project
	SOURCE_DIR ${COMPONENT_DIR}/bl2
	CONFIGURE_COMMAND ""
	BUILD_IN_SOURCE 1
	CMAKE_ARGS
	BUILD_COMMAND
		cmake
		-G Ninja
		-B ${bl2_build_dir}
		-DCMAKE_PREFIX_PATH=${CONFIG_TOOLCHAIN_PATH}
		-DCMAKE_INSTALL_PREFIX=${CMAKE_BINARY_DIR}/bl2_install
		-DIDK_PATH=${COMPONENT_DIR}/../..
		-DSOC=${target}
		-DBL2_TOOLCHAIN_FILE=toolchain_GNUARM.cmake
		-DCMAKE_BUILD_TYPE=${CONFIG_BL2_BUILD_TYPE}
		-DMCUBOOT_LOG_LEVEL=${CONFIG_BL2_LOG_LEVEL}
		-DSECURITY_HDR_DIR=$ENV{SECURITY_PREBUILD_DIR}
		-DPARTITION_HDR_DIR=$ENV{PARTITIONS_DIR}
		-DSYS_PERSIST_CONFIG_DIR=$ENV{SYS_PERSIST_CONFIG_DIR}
		-DMCUBOOT_UPGRADE_STRATEGY=${CONFIG_BL2_UPGRADE_STRATEGY}
		-DBL2_DOWNLOAD=${CONFIG_BL2_DOWNLOAD}
		-DBL2_SECURE_DEBUG=${CONFIG_SECURE_DEBUG}
		-DCONFIG_BL2_VALIDATE_ENABLED_BY_EFUSE=${CONFIG_BL2_VALIDATE_ENABLED_BY_EFUSE}
		-DCONFIG_BL2_SKIP_VALIDATE=${CONFIG_BL2_SKIP_VALIDATE}
		-DCONFIG_BL2_WDT=${CONFIG_BL2_WDT}
		-DCONFIG_AON_PMU_REG0_REFACTOR_DEV=${CONFIG_AON_PMU_REG0_REFACTOR_DEV}
		-DCONFIG_BL2_DOWNLOAD=${CONFIG_BL2_DOWNLOAD}
		-DCONFIG_BL2_WDT_PERIOD=${CONFIG_BL2_WDT_PERIOD}
		-DCONFIG_BL2_UPDATE_WITH_PC=${CONFIG_BL2_UPDATE_WITH_PC}
		-DCONFIG_RANDOM_AES_UPGRADE_BL2=${CONFIG_RANDOM_AES_UPGRADE_BL2}
		-DCONFIG_OTA_CONFIRM_UPDATE=${CONFIG_OTA_CONFIRM_UPDATE}
		-DCONFIG_OTA_UPDATE_PUBKEY=${CONFIG_OTA_UPDATE_PUBKEY}
		-DCONFIG_DBUS_CHECK_CRC=${CONFIG_DBUS_CHECK_CRC}
		-DCONFIG_DECOMPRESS_RESUME=${CONFIG_DECOMPRESS_RESUME}
		-DCONFIG_MPU=${CONFIG_MPU}
		-DCONFIG_UART_PRINT_BAUD_RATE=${CONFIG_UART_PRINT_BAUD_RATE}
		&& cd ${bl2_build_dir}
		&& ninja -- install
	BUILD_ALWAYS 1
	INSTALL_COMMAND ""
	)

add_dependencies(${COMPONENT_LIB} bl2_project)

endif()
